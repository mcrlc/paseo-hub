# Round 5, scenario 1: every factual sentence traced to the code

Head: `88dec43` (`sprite-targets/docs-security`, containing the merge of `main` at `49786ef`). Every
lifecycle slice is merged, so unlike round 1 every claim can be checked against a code path rather
than against the PRD.

Sources. **code** = this worktree at `88dec43` (`file:line`). **measured** = a QA round's own log:
`ac-vni` (activation), `ac-52w` and its `r2-README.md` (hold, handoff, release, pause), `ac-wri`
(organization env), `ac-7rt` (tick and restart), `ac-50k` (trigger edits), `ac-av6` and `ac-0yj`
(Sprites settings), `docs/qa/sprites/README.md` (the provider spikes). **r5** = produced in this
round: `r5-messages.log`, `r5-continuation.log`, `r5-yaml-compile.log`.

Status. **ok** = the code path or the measurement says what the sentence says. **ok (measured)** =
provider behaviour with no Hub code path; the figure comes from a log named in the row. **finding
Fn** = unsupported or inexact, listed in `r5-README.md`.

## docs/sprite-targets-guide.md

| Line    | Claim                                                                                                                                                                                                                                                                   | Evidence                                                                                                                                                                                                                                     | Status         |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| 3-4     | "runs a trigger's agent on a Fly Sprite that Hub creates, bootstraps, and enrolls as a daemon"                                                                                                                                                                          | `activation.ts:176-220` create → npm prefix → install paseo → bootstrap → daemon service → `paseo hub connect`                                                                                                                               | ok             |
| 4       | "The sprite pauses on its own when nothing runs on it and keeps its filesystem"                                                                                                                                                                                         | spikes 1 and 3 (`docs/qa/sprites/README.md`): an outbound socket is not activity; a warm resume keeps pid, boot id and env                                                                                                                   | ok (measured)  |
| 4-5     | "so an idle target costs storage only"                                                                                                                                                                                                                                  | PRD §2 L43 only. No QA file measures Sprites' billing                                                                                                                                                                                        | **finding F5** |
| 5-6     | "One trigger owns one sprite; two triggers on the same repository get two sprites and two checkouts."                                                                                                                                                                   | `activation.ts:107` `spriteName: trigger-${trigger.id}`; `pg.ts:5640-5642` one non-terminated sprite machine per `triggerId`                                                                                                                 | ok             |
| 6-7     | "The sprite's daemon appears on the Daemons page under the trigger's name, suffixed if that name is taken."                                                                                                                                                             | `pg.ts:1662-1698` `slugify(triggerName, hostnameSlug)`, `-${daemonId.slice(0,8)}` on conflict; `r5-messages.log` case 11. Every recreation is suffixed, because the revoked daemon keeps the slug                                            | **finding F4** |
| 9-10    | "need a Sprites org token in the organization's Sprites configuration and the sprite-targets entitlement"                                                                                                                                                               | `store.ts:91-109`, `:143-164`; `db/schema.ts:1323-1341` `organization_sprites_configuration`                                                                                                                                                 | ok             |
| 10-12   | "A self-hosted instance running without billing has the entitlement through the unlimited template; otherwise an operator grants it with an entitlement override"                                                                                                       | `provisioning.ts:31-34` `UNLIMITED_PROVISIONING`; `catalog.ts:121-126` `canUseSpriteTargets: true`; `catalog.ts:255` overrides win                                                                                                           | ok             |
| 12-13   | "saving the trigger fails with \"Sprite targets are not enabled for this organization.\""                                                                                                                                                                               | `store.ts:96-101`; `r5-messages.log` case 1, exact. Only for an **enabled** trigger (`store.ts:93`)                                                                                                                                          | **finding F6** |
| 13-14   | "without a token it fails with \"Sprites are not configured for this organization.\""                                                                                                                                                                                   | `store.ts:147-151`; `r5-messages.log` case 2, exact. Same `enabled` qualifier                                                                                                                                                                | **finding F6** |
| 18-19   | "Omitting it, or `kind: daemon`, keeps the `{ daemon, cwd, worktree }` target"                                                                                                                                                                                          | `triggers/configuration/schema.ts:85-88` (`kind` defaults to `daemon`)                                                                                                                                                                       | ok             |
| 23      | `kind` required, `sprite`                                                                                                                                                                                                                                               | `schema.ts:93`                                                                                                                                                                                                                               | ok             |
| 24      | `bootstrap` "run once after the sprite is created, as the `sprite` user"                                                                                                                                                                                                | `schema.ts:94` required; `activation.ts:198-205` one exec; spike 6 "Runs as `sprite`"                                                                                                                                                        | ok             |
| 24      | "Hub installs the Paseo CLI before it, and starts the daemon and enrolls it after it."                                                                                                                                                                                  | `activation.ts:187-220`: `npm install -g @getpaseo/cli`, then bootstrap, then `provider.service`, then `hub connect`                                                                                                                         | ok             |
| 24      | "A non-zero exit fails the sprite."                                                                                                                                                                                                                                     | `activation.ts:410-414` `expectSuccess`; `:134-142` terminate, revoke, destroy                                                                                                                                                               | ok             |
| 25      | `cwd` "Absolute directory on the sprite"; "Hub does not clone."                                                                                                                                                                                                         | `schema.ts:95` `startsWith("/")`; `activation.ts` has no clone                                                                                                                                                                               | ok             |
| 26      | `memory` "Defaults to the organization's configured default memory, which is 8192 MB unless the organization changed it"                                                                                                                                                | `activation.ts:96`, `:247` `target.memory ?? configuration.memoryMb`; `db/schema.ts:1330` `memory_mb` default 8192; `ac-0yj/4-memory-default.txt`                                                                                            | ok             |
| 26      | "It is the only shape setting; Sprites offers no CPU or region choice."                                                                                                                                                                                                 | `client.ts:127-137` create takes name and memory only; spike "Resources and network policy": `cpu` is rejected with `unknown field "cpu"`                                                                                                    | ok             |
| 27      | `env` "Per-trigger, non-secret values for the daemon service and `bootstrap` environment."                                                                                                                                                                              | `activation.ts:195-206` `targetEnv` reaches both                                                                                                                                                                                             | ok             |
| 27      | "Never secrets: they are not redacted from logs"                                                                                                                                                                                                                        | `activation.ts:117` the scrub list is the enrollment key plus the organization env only; `r5-messages.log` case 10 keeps `target-plain` in the reason                                                                                        | ok             |
| 27      | "provider credentials belong in the organization's daemon environment"                                                                                                                                                                                                  | `activation.ts:206`, `:296-298`; `settings.ts:92-106`                                                                                                                                                                                        | ok             |
| 27      | "A connection template here fails activation."                                                                                                                                                                                                                          | `activation.ts:354-372` throws; `:134-142` terminates the machine; `r5-messages.log` case 9                                                                                                                                                  | ok             |
| 28      | `worktree` "Same schema and behaviour as on a daemon target."                                                                                                                                                                                                           | `config/compiler.ts:141`, `:152` the same `WorktreeTargetSchema`                                                                                                                                                                             | ok             |
| 30-52   | YAML example 1 compiles                                                                                                                                                                                                                                                 | `r5-yaml-compile.log` block 1: sprite environment, `memory: 16384`, `worktree.newBranch` kept as a template                                                                                                                                  | ok             |
| 54-55   | "`auto_archive` must stay `true` … saving `false` fails with \"Sprite targets always archive; auto_archive must be true.\""                                                                                                                                             | `triggers/configuration/schema.ts:188-194`; `r5-yaml-compile.log` control; `r5-messages.log` case 3, exact                                                                                                                                   | ok             |
| 55-56   | "Hub does not control when the provider drops a paused sprite's memory, and only an archived workspace is guaranteed to restore."                                                                                                                                       | spike 3 (a cold wake kept process memory in that run; the docs' "processes start fresh" case appeared only after a checkpoint restore)                                                                                                       | ok (measured)  |
| 58-59   | "`HOME`, `PATH`, `PASEO_HOME`, and `PASEO_PASSWORD` are refused in the target's `env`, because Hub sets them on every sprite."                                                                                                                                          | `config/compiler.ts:1192-1199` calls `spritesEnvKeyError`; `sprites/env.ts:2-10`; `activation.ts:374-389` sets all four; `r5-messages.log` cases 4                                                                                           | ok             |
| 61-62   | "the provider pauses a sprite about a second after Hub releases the last hold on it"                                                                                                                                                                                    | `ac-52w/r2-README.md`: release → pause 0.6-1.4 s over three runs                                                                                                                                                                             | ok (measured)  |
| 62-63   | "`run.idle_timeout` still applies per execution, as on any target."                                                                                                                                                                                                     | `triggers/configuration/schema.ts:117`; `workflows/engine.ts:295-301`, `:1035`                                                                                                                                                               | ok             |
| 67-69   | Activation "creates the sprite, installs the Paseo CLI, runs `bootstrap`, writes the daemon service, and enrolls the daemon, so a bad `bootstrap` fails at activation rather than on the first event."                                                                  | `activation.ts:176-220` in that order; `:134-142` on failure                                                                                                                                                                                 | ok             |
| 69      | "The sprite then pauses."                                                                                                                                                                                                                                               | `ac-vni` finding 5: warm 50 s after alive                                                                                                                                                                                                    | ok (measured)  |
| 69      | "The first boot takes about a minute"                                                                                                                                                                                                                                   | Save → alive measured 27 s (`ac-vni`), 29 s (`ac-wri`), 29.1 s (`ac-7rt`), 33-38 s (`ac-50k`)                                                                                                                                                | **finding F1** |
| 69-70   | "the daemon's first start downloads about 1 GB of speech models"                                                                                                                                                                                                        | spike 4a: `PASEO_HOME` 985 MB after first boot, almost all speech models                                                                                                                                                                     | ok (measured)  |
| 71-72   | "Hub holds the sprite, which wakes it, and defers the run until the daemon's socket is live."                                                                                                                                                                           | `lifecycle.ts:271-290` hold through exec (spike 2: exec wakes); `workflows/engine.ts:674-677` `canDispatchToDaemon`; `app.ts:189-190` is the live connection                                                                                 | ok             |
| 72      | "From a warm pause the daemon reconnects in about a second."                                                                                                                                                                                                            | `ac-52w/r2-README.md` scenario 2: hold 12:15:25.015, reconnect 12:15:25.782 (767 ms), handoff 789 ms                                                                                                                                         | ok (measured)  |
| 72-73   | "A cold sprite, about ten minutes after the pause, takes about a minute."                                                                                                                                                                                               | `ac-52w` finding 10: cold after about 10 min warm, exec from cold 64 s to a daemon reconnect                                                                                                                                                 | ok (measured)  |
| 73      | "The wait is bounded by `max_runtime`."                                                                                                                                                                                                                                 | `workflows/engine.ts:196` run deadline = `createdAt + maxRuntimeMs`; `:467-471` a wakeup past the deadline stops deferring                                                                                                                   | ok             |
| 74      | "Event while the sprite is awake. Hub holds it and hands off."                                                                                                                                                                                                          | `lifecycle.ts:271-291` the same path, hold then ready                                                                                                                                                                                        | ok             |
| 75-76   | "Hub waits for the terminal hub action, the archive, to complete and releases the hold only then"                                                                                                                                                                       | `lifecycle.ts:1433-1450` the release is a `finally` around the control attempt; `ac-52w/r2-README.md`: archive complete precedes release by 244-301 ms in all three runs                                                                     | ok             |
| 76      | "the provider pauses the sprite about a second later"                                                                                                                                                                                                                   | `ac-52w/r2-README.md`: release → pause 0.6-1.4 s                                                                                                                                                                                             | ok (measured)  |
| 76-77   | "Releasing earlier would let the pause cut the archive short."                                                                                                                                                                                                          | `ac-52w` round 1 finding 1: the earlier order lost the archive, which completed only on the next wake                                                                                                                                        | ok (measured)  |
| 77      | "Each execution holds the sprite under its own name, so a sibling execution keeps it awake."                                                                                                                                                                            | `lifecycle.ts:274` the task name is the execution id; `client.ts:153-160` `PUT`/`DELETE /v1/tasks/:name`                                                                                                                                     | ok             |
| 78      | "`bootstrap` changed. Hub destroys the sprite, revokes its daemon, and terminates its machine row."                                                                                                                                                                     | `activation.ts:237-244` → `retireSprite`, `:270-281` destroy, `revokeDaemon`, `transitionMachine("terminated")` in that order                                                                                                                | ok             |
| 78-79   | "The next event creates a new sprite"                                                                                                                                                                                                                                   | `lifecycle.ts:249-263` recreates once per execution, then defers                                                                                                                                                                             | ok             |
| 79-81   | "because the daemon identity is new, a fresh conversation; the run records the session reset"                                                                                                                                                                           | `agent-sessions/index.ts:72-74`, `:169-175` `retireSession` returns `"reset"` for a different daemon; `ac-50k` finding 6: four recreations, all `agent_session_action = reset`                                                               | ok             |
| 80-81   | "The filesystem, including the checkout and the agent's history, does not carry over."                                                                                                                                                                                  | `client.ts:162-164` `DELETE /v1/sprites/:n`; spike 6 destroy → 404                                                                                                                                                                           | ok             |
| 82-83   | "`env` changed. Hub rewrites the daemon service, which restarts the daemon. The sprite, its filesystem, and its daemon identity are kept."                                                                                                                              | `activation.ts:257-266`, `:283-303`; `client.ts:145-151` `DELETE` then `PUT`; `ac-wri` scenario 3: pid 725 → 789, daemon offline 4.7 s, same daemon row                                                                                      | ok             |
| 82-84   | `env` and `memory` edits described with no effect on a running conversation                                                                                                                                                                                             | `workflows/engine.ts:1046-1056` puts the whole compiled environment in `continuation.compatibility`; `agent-sessions/index.ts:97-101` throws; `r5-continuation.log` reproduces the failure                                                   | **finding F2** |
| 84      | "`memory` changed. Hub updates the resources policy in place."                                                                                                                                                                                                          | `activation.ts:250-256` `setMemory`; `client.ts:127-131` `POST /v1/sprites/:n/policy/resources`                                                                                                                                              | ok             |
| 85-86   | "Trigger disabled, or switched to a daemon target. Hub destroys the sprite."                                                                                                                                                                                            | `store.ts:80-84` passes `target: undefined`; `activation.ts:237-244` retires                                                                                                                                                                 | ok             |
| 86      | "Re-enabling the trigger activates a new one immediately, without waiting for an event."                                                                                                                                                                                | `activation.ts:75-88` the terminated row falls through to the create branch; `ac-50k` finding 5: alive 36 s after the save with no run fired                                                                                                 | ok             |
| 87      | "Daemon revoked. Hub destroys the sprite and terminates the row; the next event creates a new one."                                                                                                                                                                     | `lifecycle.ts:1527-1536` `destroySprite`; `ac-50k` scenario 6                                                                                                                                                                                | ok             |
| 89-90   | "\"Changing bootstrap recreates the sprite and ends its 1 running execution. Save again once it is idle.\""                                                                                                                                                             | `store.ts:124-141`, `:177-196`; `r5-messages.log` case 6, exact, singular and plural                                                                                                                                                         | ok             |
| 90-91   | "Disabling the trigger and switching its target are refused the same way."                                                                                                                                                                                              | `store.ts:183-190`; `r5-messages.log` cases 7 and 8, exact                                                                                                                                                                                   | ok             |
| 91      | "Edits that keep the sprite, `env` and `memory`, are not."                                                                                                                                                                                                              | `store.ts:191` returns undefined when the bootstrap hash matches; `r5-messages.log` both saves accepted with two executions running                                                                                                          | ok             |
| 93-94   | "A tick every five minutes re-holds every execution that is still spawning or running, which also recreates a hold the provider lost."                                                                                                                                  | `lifecycle.ts:82`, `:1025-1039`, `:1041-1049`; `pg.ts:2384` the query is `status in ('spawning','running')`; `ac-7rt`: tick at exactly 300 s, and a deleted task recreated on the next tick                                                  | ok             |
| 94-96   | "When Hub restarts it re-holds from the execution rows and resolves every sprite still in `spawning`: if its daemon enrolled, the row becomes alive; otherwise Hub terminates the row, revokes the enrollment key, and destroys the half-built sprite"                  | `lifecycle.ts:999-1023` in that order; `ac-7rt` scenario 3 (re-hold 1.4 s after start) and scenario 4 (row resolved in 1.32 s, key revoked, sprite 404)                                                                                      | ok             |
| 98-99   | "A run that reaches a sprite whose daemon is not connected yet waits rather than fails, exactly as for an offline daemon target"                                                                                                                                        | `lifecycle.ts:267-270` deferred; `workflows/engine.ts:674-677` the same `canDispatchToDaemon` gate a daemon target uses                                                                                                                      | ok             |
| 99-101  | "If the sprite is gone and recreating it also fails, the run fails as `sprite_unavailable` instead of waiting out `max_runtime`."                                                                                                                                       | `lifecycle.ts:248-265`; `workflows/engine.ts:542-550`. A run whose trigger row cannot be found fails the same way without a recreation attempt                                                                                               | ok             |
| 105-106 | "Continuation keys an agent by trigger and conversation. Two triggers never share an agent, even on the same pull request."                                                                                                                                             | `pg.ts:2938-2946` sessions are keyed `(project_id, continuation_key)`; `pg.ts:3108-3128` every trigger gets its own runtime project                                                                                                          | ok             |
| 108-133 | YAML example 2 compiles                                                                                                                                                                                                                                                 | `r5-yaml-compile.log` block 2: two events with their filters, `continuation: {mode: conversation}`, `${{ paseo.context }}` in the prompt                                                                                                     | ok             |
| 135-136 | "Both events on one pull request derive the same conversation key, so they land on the same agent."                                                                                                                                                                     | `triggers/github/provider.ts:346-359` the key is `["github", repositoryId, number]` for both the `pull_request` and the `issue` payload                                                                                                      | ok             |
| 138-139 | "When the comment arrives while the review is still running, it is sent to the running agent as a new message."                                                                                                                                                         | `agent-sessions/index.ts:141-156` an existing session with an `agentId` is `continued`, then `deliver` sends                                                                                                                                 | ok             |
| 139-140 | "When it arrives after the review ended, Hub holds the sprite, restores the archived workspace, and sends the message to the same agent."                                                                                                                               | `lifecycle.ts:271-290` hold first; `agent-sessions/index.ts:157-161` `restore(workspaceId)` when `archivedAt`; spike 5 run-4 proves it end to end                                                                                            | ok             |
| 140-141 | "An agent that closed or errored fails the run as `agent_interrupted`."                                                                                                                                                                                                 | `agent-sessions/index.ts:154-156`; `lifecycle.ts:2064-2066` the message becomes the failure reason                                                                                                                                           | ok             |
| 145-147 | "A trigger with a leased credential, either `run.github` or a connection template in `run.env`, continues an agent only while one of its executions is active. After every execution finishes, the next event starts a new agent. This is Hub's rule for every target." | `agent-sessions/index.ts:283-290` `hasTemporaryCredentials`, `:61-74` `isFinishedCredentialedSession`. The sprite target's own `env` is not in the launch intent (`workflows/engine.ts:1008-1018`), so it cannot make a session credentialed | ok             |
| 149-150 | "Provider credentials for sprites go in Settings → Sprites → daemon environment, for example `CLAUDE_CODE_OAUTH_TOKEN`"                                                                                                                                                 | `settings.ts:92-106`; spike 5 used exactly that variable and the daemon-spawned agent inherited it                                                                                                                                           | ok             |
| 150-151 | "The page keeps values write-only: it lists the names it holds and never shows a value again."                                                                                                                                                                          | `settings.ts:60-70` the snapshot returns `envKeys` only; `ac-av6/1-values-never-reach-client.txt`                                                                                                                                            | ok             |
| 151-154 | "Hub writes this environment into the daemon service of every sprite in the organization, and into the `bootstrap` environment, beneath the target's `env`; on a clash the target's value wins."                                                                        | `activation.ts:196` and `:206` both spread `{...organizationEnv, ...targetEnv}`; `ac-wri` scenario 2: `QA_CLASH=from-target` in the service, the environ and the bootstrap env                                                               | ok             |
| 154     | "Values of eight characters or more are redacted from Hub's logs and failure reasons."                                                                                                                                                                                  | `activation.ts:417-425` `SECRET_MIN_LENGTH = 8`; `r5-messages.log` case 10: a 7-character value survives, an 8-character one becomes `[redacted]`                                                                                            | ok             |
| 154-155 | "`HOME`, `PATH`, `PASEO_HOME`, and `PASEO_PASSWORD` are refused here too."                                                                                                                                                                                              | `settings.ts:95-96` → `sprites/env.ts:5-11`; `r5-messages.log` cases 5                                                                                                                                                                       | ok             |
| 155     | "Nothing secret enters trigger YAML."                                                                                                                                                                                                                                   | Guidance, consistent with the above                                                                                                                                                                                                          | ok             |
| 157-158 | "Saving the daemon environment rewrites the service of every alive sprite in the organization, one at a time … and the save waits for all of them."                                                                                                                     | `settings.ts:105`, `:115` await the rewrite; `activation.ts:318-347` a serial loop skipping anything not `alive`                                                                                                                             | ok             |
| 158     | "about seven seconds each"                                                                                                                                                                                                                                              | `ac-wri` finding 3: 6.66 s, 7.05 s, 6.68 s, and its own summary "about 6 s per live sprite"; `ac-50k` finding 7: 6.130 s                                                                                                                     | **finding F7** |
| 158     | "waking a paused sprite to do it"                                                                                                                                                                                                                                       | `ac-wri` scenario 5A: the service `PUT` set a warm sprite `running`                                                                                                                                                                          | ok (measured)  |
| 159-160 | "Each rewrite restarts that sprite's daemon, which reconnects on its own. Rotating a credential is therefore a settings save, not a recreation."                                                                                                                        | `client.ts:145-151` `DELETE` then `PUT`; `ac-wri` scenario 3: daemon offline 4.7 s and 4.6 s, same machine row                                                                                                                               | ok             |
| 162-164 | "A `${{ paseo.connections.<slug>.<value> }}` template there fails activation with a message naming the key, because every connection kind Hub has needs an execution lease."                                                                                            | `activation.ts:354-372`; `r5-messages.log` case 9. True for a GitHub connection, the only kind with a resolver (`application-runtime.ts:429-457`, `providers/github/index.ts:166-190`)                                                       | **finding F3** |
| 166-168 | "Credentials in the daemon environment are not leases. They live in the daemon service for the life of the sprite and every agent on it inherits them"                                                                                                                  | `activation.ts:374-390` the service definition holds them; spike 5 proves inheritance                                                                                                                                                        | ok             |
| 168-170 | "A reviewer that must remember a pull request from one day to the next takes provider credentials from the daemon environment and omits `run.github`."                                                                                                                  | `agent-sessions/index.ts:283-290`: `intent.github` alone makes the session credentialed                                                                                                                                                      | ok             |
| 172-173 | "Sprites hold no interactive provider logins. A `bootstrap` that needs one is a misconfiguration"                                                                                                                                                                       | `activation.ts:198-205` bootstrap is one non-interactive `sh -s` exec; spike 6 exec is not a TTY                                                                                                                                             | ok             |
| 177-179 | "The daemon detects agent providers when it starts, and it starts after `bootstrap`. Installing Claude Code later … leaves every run failing with `Provider 'claude' is not available`"                                                                                 | `activation.ts:197-208` bootstrap precedes the service write; `ac-52w` finding 8 saw exactly that message until the sprite was recreated                                                                                                     | ok (measured)  |
| 180-182 | "The package's postinstall does not run on a sprite, so its native binary is missing until you run `node …/install.cjs`"                                                                                                                                                | spike 4a gotchas                                                                                                                                                                                                                             | ok (measured)  |
| 183-186 | "Hub runs `bootstrap` with the npm global bin directory on `PATH`, plus the organization's daemon environment and the target's `env` … `sprite exec` … is non-login and its `PATH` lacks the npm global bin directory"                                                  | `activation.ts:186`, `:196`; spike 4a and spike 6 ("Runs as `sprite`, non-login `PATH`")                                                                                                                                                     | ok             |
| 187-191 | "Clone in `bootstrap`. Hub does not clone. For a private repository, put the clone credential in Settings → Sprites → daemon environment"                                                                                                                               | `activation.ts` has no clone; `:196` the bootstrap env carries the organization env; spike 7: a Sprites connector covers REST but not `git clone`                                                                                            | ok             |
| 195     | "One sprite per trigger. A burst of events on one trigger runs on one machine."                                                                                                                                                                                         | `pg.ts:5640-5642`, `activation.ts:107`                                                                                                                                                                                                       | ok             |
| 196-197 | "One Hub instance per database. Restart recovery treats every sprite still in `spawning` as its own"                                                                                                                                                                    | `pg.ts:291` the query has no instance or organization predicate; `ac-7rt` finding 4                                                                                                                                                          | ok             |
| 198     | "Legacy multi-step bundles cannot target sprites."                                                                                                                                                                                                                      | `config/compiler.ts:157` the bundle union is daemon only; `:304-305`, `:444` `spriteTargets` is set by `compileTriggerDocument` alone                                                                                                        | ok             |
| 199     | "A hold lasts one hour and the tick refreshes it every five minutes."                                                                                                                                                                                                   | `lifecycle.ts:274`, `:1033` `"60m"`; `:82` 5 min; spike 2: `expire` above 3600 s is rejected                                                                                                                                                 | ok             |
| 199-200 | "If Hub is down longer than a hold's remaining time, the sprite can pause mid-turn; the turn resumes on the next hold."                                                                                                                                                 | spike 3 warm resume: same pid, same boot id, guest clock corrected                                                                                                                                                                           | ok (measured)  |
| 201-202 | "A daemon environment save is serial across the organization's sprites, about seven seconds per alive sprite."                                                                                                                                                          | Same as L158                                                                                                                                                                                                                                 | **finding F7** |

## SECURITY.md

| Line | Claim                                                                                                                                                                                | Evidence                                                                                                                                                                                                    | Status |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 7    | "runs its agent on a Fly Sprite that Hub creates in the Sprites organization whose token the Hub organization configured"                                                            | `activation.ts:89-92`, `:116` the provider is built from that organization's token; `db/schema.ts:1323-1327` keyed by `organization_id`. Round 1's F6 is answered by this wording                           | ok     |
| 9    | "A sprite runs untrusted trigger input … with whatever the authored `bootstrap` installed and every credential it placed on the sprite."                                             | `activation.ts:196-206`; the prompt carries `${{ paseo.context }}` (guide example 2)                                                                                                                        | ok     |
| 10   | "Hub stores the Sprites org token, and the organization's daemon environment, as plain text in the `organization_sprites_configuration` table. There is no secret encryption layer." | `db/schema.ts:1329-1331`: `token` is `text`, `env` is `jsonb`, neither encrypted; no encryption layer exists in `src/`                                                                                      | ok     |
| 10   | "The org token can execute commands in every sprite in the organization, read and write their filesystems, and destroy them."                                                        | `client.ts:62-80` every call is that one bearer; spike 6 exec and destroy                                                                                                                                   | ok     |
| 11   | "The organization's daemon environment … is written into every sprite's daemon service and into the environment of its `bootstrap` run."                                             | `activation.ts:196`, `:206`, `:296-298`; `ac-wri` scenario 2                                                                                                                                                | ok     |
| 11   | "It and any long-lived GitHub credential placed on the sprite are not per-execution leases: they live as long as the sprite and reach every agent on it"                             | `activation.ts:374-390`; spike 5 inheritance                                                                                                                                                                | ok     |
| 11   | "the target's own `env` is not treated as secret and is not redacted from logs, while values of eight characters or more from the daemon environment are"                            | `activation.ts:117`, `:416-424`; `r5-messages.log` case 10                                                                                                                                                  | ok     |
| 11   | "A trigger that uses `run.github` instead gets leased tokens revoked at terminal and gives up continuation across idle days."                                                        | `execution-authority/index.ts:145-148` registers the lease; `agent-sessions/index.ts:61-74`, `:283-290`                                                                                                     | ok     |
| 12   | "Its URL stays organization-only, no service binds an HTTP port, and all traffic is the daemon's outbound connection to Hub."                                                        | `activation.ts:385` `--listen 127.0.0.1:6767 --no-relay --no-web-ui`; no `http_port` is ever set (`client.ts:134-151`); spike 2: the URL is `auth: sprite` and an unauthenticated GET does not even wake it | ok     |
| 12   | "`PASEO_PASSWORD` is set, so a misconfiguration that exposes the daemon port does not leave it open."                                                                                | `activation.ts:379` 32 random bytes per service write; `ac-wri` scenario 2 read a 43-character value out of `/proc/<pid>/environ`                                                                           | ok     |
| 13   | "Hub enrolls each sprite with an API key scoped to `daemons:enroll` only and revokes it after enrollment."                                                                           | `activation.ts:93-95`; `pg.ts:1642-1652` revokes the key inside the enrollment transaction; `ac-vni` scenario 3                                                                                             | ok     |
